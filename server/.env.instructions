# GitHub OAuth Configuration
# Get these from your GitHub OAuth App settings
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# The redirect URI must match exactly what you set in GitHub OAuth App settings
# For local development, this should point to your frontend callback page
GITHUB_REDIRECT_URI=http://localhost:5173/oauth/callback
